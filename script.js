function openCatalog(name) {
  document.getElementById('catalog-' + name).style.display = 'flex';
}

function closeCatalog(name) {
  document.getElementById('catalog-' + name).style.display = 'none';
}
